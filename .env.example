# ─── Invoka MCP Server ─────────────────────────────────────────────────────────
# Copia este archivo como .env y completa los valores REALES antes de usar.
#   cp .env.example .env && chmod 600 .env
# ─────────────────────────────────────────────────────────────────────────────

# API Key obtenida desde el panel de invoka.com.ec.
# Se envía en el header X-API-KEY de cada petición.
INVOKA_API_KEY=

# (Opcional) URL base de la API. No modificar salvo indicación de Invoka.
# INVOKA_BASE_URL=https://www.invoka.com.ec

# (Opcional) Timeout HTTP en segundos.
# INVOKA_HTTP_TIMEOUT=30

# Opciones: sse | http_stream
MCP_TRANSPORT_MODE=sse
MCP_PORT=8000
